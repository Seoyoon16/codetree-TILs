#include <iostream>

int main() {
    // 여기에 코드를 작성해주세요.
    using namespace std;
    
    char c;
    double a, b;
    cin >> c >> a >> b;
    cout << fixed;
    cout.precision(2);
    cout << c << endl << a << endl << b;

    return 0;
}