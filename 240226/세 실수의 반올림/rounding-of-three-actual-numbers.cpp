#include <iostream>

int main() {
    // 여기에 코드를 작성해주세요.
    using namespace std;

    double a, b, c;
    cin >> a >> b >> c;
    cout << fixed;
    cout.precision(3);

    cout << a << endl << b << endl << c;
    return 0;
}